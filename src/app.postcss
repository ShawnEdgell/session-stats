@tailwind base;
@tailwind components;
@tailwind utilities;
@tailwind variants;

html, body {
    @apply h-full overscroll-none scroll-smooth;
    scrollbar-gutter: stable both-edges;
}

h1, h2, h3, h4, h5, h6 {
    @apply font-bold;
}

.container {
    @apply space-y-3;
}